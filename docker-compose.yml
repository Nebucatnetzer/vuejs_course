version: "3"

services:
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - 8080:8080
    volumes:
      - ./2021-05-05_vuex-11-a-challenge-starting-code/:/app
